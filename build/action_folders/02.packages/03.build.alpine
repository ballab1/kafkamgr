# kafka-manager build Packages

declare -a OPTS=( '--virtual' '.buildDependencies' )
declare -a PKGS=( git 
                  gnupg 
                  tar 
                  unzip 
                  wget )
